# Google 行事曆 API（必填才能顯示已預約時段與寫入預約）
# 1. 到 https://console.cloud.google.com/ 建立專案，啟用 Google Calendar API
# 2. 建立「服務帳戶」，下載 JSON 金鑰
# 3. 將「錄音室」用的 Google 行事曆與該服務帳戶的 email 共用（編輯權限）
# 4. 將下面變數複製到 .env.local 並填入

# 兩間錄音室的行事曆 ID
GOOGLE_CALENDAR_BIG=xxx@group.calendar.google.com
GOOGLE_CALENDAR_SMALL=yyy@group.calendar.google.com
# 若未設上面兩個，可只用一個（兩間共用）
# GOOGLE_CALENDAR_ID=primary

# 服務帳戶 JSON 檔案路徑（大間、小間可用不同服務帳戶）
GOOGLE_APPLICATION_CREDENTIALS_BIG=./big-room-credentials.json
GOOGLE_APPLICATION_CREDENTIALS_SMALL=./small-room-credentials.json
# 試算表（KOL、使用記錄）若未設 GOOGLE_APPLICATION_CREDENTIALS_SHEETS，會用 BIG 或下方共用
# GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json

# KOL 折扣碼試算表（從試算表網址 docs.google.com/spreadsheets/d/【這裡】/edit 取得）
# GOOGLE_SHEET_ID=xxx
